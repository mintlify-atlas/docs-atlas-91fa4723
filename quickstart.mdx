---
title: Quick Start
description: Get started with Stock Request in minutes. Create your first stock request and see the procurement process in action.
---

## Overview

This guide will walk you through creating your first stock request and understanding the complete workflow from request creation to fulfillment.

<Info>
Before starting, ensure you have:
- Installed the Stock Request module
- Been assigned to "Stock Request / User" or "Stock Request / Manager" group
- At least one warehouse and location configured
</Info>

## Creating Your First Stock Request

<Steps>
  <Step title="Navigate to Stock Requests">
    From the main menu, click **Stock Requests** → **Stock Requests**
    
    You'll see a list view of all stock requests (filtered by your access rights).
  </Step>

  <Step title="Click New to create a request">
    Click the **New** button in the top-left corner.
    
    A form will open with the following fields:
  </Step>

  <Step title="Fill in the request details">
    <Tabs>
      <Tab title="Required Fields">
        **Product** 
        Select the product you need. Only storable products are available.
        
        ```python
        # Field: product_id
        # Type: Many2one('product.product')
        ```
        
        **Quantity**
        Enter the quantity you need.
        
        ```python
        # Field: product_uom_qty
        # Type: Float
        # Must be > 0
        ```
        
        **UoM** (Unit of Measure)
        Automatically filled based on product. Can be changed if needed.
        
        ```python
        # Field: product_uom_id
        # Type: Many2one('uom.uom')
        ```
        
        **Location**
        Where you want the stock delivered.
        
        ```python
        # Field: location_id
        # Type: Many2one('stock.location')
        # Default: Warehouse's stock location
        ```
        
        **Warehouse**
        The warehouse that will process your request.
        
        ```python
        # Field: warehouse_id
        # Type: Many2one('stock.warehouse')
        ```
      </Tab>
      
      <Tab title="Optional Fields">
        **Expected Date**
        When you need the stock by (defaults to now).
        
        ```python
        # Field: expected_date
        # Type: Datetime
        # Default: fields.Datetime.now()
        ```
        
        **Shipping Policy**
        - *Receive each product when available*: Partial deliveries allowed
        - *Receive all products at once*: Wait for complete quantity
        
        ```python
        # Field: picking_policy
        # Default: 'direct'
        ```
        
        **Route**
        Specific route to use (optional - system will auto-select).
        
        ```python
        # Field: route_id
        # Type: Many2one('stock.route')
        ```
        
        **Procurement Group**
        Group related movements together.
        
        ```python
        # Field: procurement_group_id
        # Type: Many2one('procurement.group')
        ```
      </Tab>
    </Tabs>
  </Step>

  <Step title="Save the request">
    Click **Save**
    
    The system will:
    - Generate a unique reference number (e.g., `SR/00001`)
    - Set the state to **Draft**
    - Set you as the **Requested By** user
    
    <Note>
    The request can be edited freely while in Draft state.
    </Note>
  </Step>
</Steps>

## Example: Simple Request

<CodeGroup>
  ```python Python API
  # Create a stock request programmatically
  stock_request = env['stock.request'].create({
      'product_id': env.ref('product.product_product_27').id,  # PC1 product
      'product_uom_qty': 5.0,
      'product_uom_id': env.ref('uom.product_uom_unit').id,
      'location_id': env.ref('stock.stock_location_stock').id,
      'warehouse_id': env.ref('stock.warehouse0').id,
      'expected_date': fields.Datetime.now(),
      'picking_policy': 'direct',
  })
  
  print(f"Created request: {stock_request.name}")
  # Output: Created request: SR/00001
  ```

  ```xml XML-RPC
  <!-- XML-RPC example -->
  <methodCall>
    <methodName>execute</methodName>
    <params>
      <param><string>database</string></param>
      <param><int>user_id</int></param>
      <param><string>password</string></param>
      <param><string>stock.request</string></param>
      <param><string>create</string></param>
      <param>
        <struct>
          <member>
            <name>product_id</name>
            <value><int>27</int></value>
          </member>
          <member>
            <name>product_uom_qty</name>
            <value><double>5.0</double></value>
          </member>
          <member>
            <name>location_id</name>
            <value><int>8</int></value>
          </member>
        </struct>
      </param>
    </params>
  </methodCall>
  ```
</CodeGroup>

## Confirming the Request

Once your request is ready, confirm it to trigger the procurement process.

<Steps>
  <Step title="Click Confirm">
    Click the **Confirm** button at the top of the form.
    
    The system will:
    1. Validate the request (quantity > 0, valid product, etc.)
    2. Change state from **Draft** to **In Progress**
    3. Run the procurement engine
  </Step>

  <Step title="Procurement engine evaluates">
    The system checks:
    
    <Accordion title="Is 'Check Available Stock First' enabled?">
      If `company.stock_request_check_available_first == True`:
      
      ```python
      # Check if stock is available in the location
      if product.with_context(location=location_id).free_qty >= product_uom_qty:
          # Create and execute immediate transfer
          self._action_use_stock_available()
      else:
          # Run procurement rules
          self._action_launch_procurement_rule()
      ```
    </Accordion>
    
    <Accordion title="Which route should be used?">
      The system selects routes in this priority:
      
      1. Route specified on the stock request
      2. Routes on the product's "Routes" field
      3. Routes configured on the warehouse
      4. Global routes matching the destination location
      
      ```python
      def _prepare_procurement_values(self, group_id=False):
          return {
              'date_planned': self.expected_date,
              'warehouse_id': self.warehouse_id,
              'route_ids': self.route_id,
              'group_id': group_id or self.procurement_group_id.id,
          }
      ```
    </Accordion>
    
    <Accordion title="What action should be triggered?">
      Based on the selected route's rules:
      
      - **pull/push**: Create stock moves and transfers
      - **buy**: Create purchase order (requires `stock_request_purchase`)
      - **manufacture**: Create manufacturing order (requires `stock_request_mrp`)
    </Accordion>
  </Step>

  <Step title="Review created operations">
    After confirmation, click the **Transfers** button to view:
    - Stock pickings
    - Purchase orders (if applicable)
    - Manufacturing orders (if applicable)
    
    The smart button shows the count of related operations.
  </Step>
</Steps>

## Understanding Request States

Stock requests progress through these states:

<Steps>
  <Step title="Draft">
    **Initial state** when the request is created.
    
    - Request can be edited or deleted
    - No procurement has been triggered
    - No stock movements exist
    
    **Available actions:**
    - Edit any field
    - Delete the request
    - Confirm to move to next state
  </Step>

  <Step title="In Progress">
    **Active state** after confirmation.
    
    - Procurement has been triggered
    - Stock movements, POs, or MOs are created
    - Request is being fulfilled
    
    **Available actions:**
    - Cancel the request (cancels all pending moves)
    - View transfers/orders
    - Limited editing (some fields locked)
    
    **Quantity tracking:**
    ```python
    # Three computed quantities track progress
    qty_in_progress  # Quantity being processed
    qty_done         # Quantity completed
    qty_cancelled    # Quantity cancelled
    ```
  </Step>

  <Step title="Done">
    **Completed state** when all stock has been received.
    
    - All requested quantity has been fulfilled
    - All transfers are completed
    - Request is read-only
    
    **Auto-transition:**
    ```python
    # System automatically moves to Done when
    if qty_done >= product_uom_qty:
        state = 'done'
    ```
  </Step>

  <Step title="Cancelled">
    **Terminal state** when request is cancelled.
    
    - All pending moves are cancelled
    - No further action possible
    - Request is read-only
    
    **Manual action:**
    Click **Cancel** button to cancel the request and all pending operations.
  </Step>
</Steps>

## Working Example: Internal Transfer

Let's walk through a complete example with stock available.

<Steps>
  <Step title="Setup: Ensure stock is available">
    ```python
    # Add stock to source location
    product = env['product.product'].create({
        'name': 'Widget A',
        'type': 'product',
        'default_code': 'WGT-A',
    })
    
    # Add 100 units to WH/Stock
    quant = env['stock.quant'].create({
        'product_id': product.id,
        'location_id': env.ref('stock.stock_location_stock').id,
        'quantity': 100.0,
    })
    ```
  </Step>

  <Step title="Create destination location">
    ```python
    production_loc = env['stock.location'].create({
        'name': 'Production Floor',
        'location_id': env.ref('stock.warehouse0').view_location_id.id,
        'usage': 'internal',
    })
    ```
  </Step>

  <Step title="Configure route and rule">
    ```python
    # Create route
    route = env['stock.route'].create({
        'name': 'WH to Production',
        'warehouse_ids': [(4, env.ref('stock.warehouse0').id)],
    })
    
    # Create rule
    rule = env['stock.rule'].create({
        'name': 'Transfer to Production',
        'route_id': route.id,
        'location_src_id': env.ref('stock.stock_location_stock').id,
        'location_dest_id': production_loc.id,
        'action': 'pull',
        'picking_type_id': env.ref('stock.warehouse0').int_type_id.id,
        'procure_method': 'make_to_stock',
        'warehouse_id': env.ref('stock.warehouse0').id,
    })
    
    # Assign route to product
    product.route_ids = [(4, route.id)]
    ```
  </Step>

  <Step title="Create and confirm request">
    ```python
    # Create stock request
    request = env['stock.request'].create({
        'product_id': product.id,
        'product_uom_qty': 10.0,
        'location_id': production_loc.id,
        'warehouse_id': env.ref('stock.warehouse0').id,
    })
    
    print(f"Request created: {request.name}, State: {request.state}")
    # Output: Request created: SR/00001, State: draft
    
    # Confirm the request
    request.action_confirm()
    
    print(f"State after confirm: {request.state}")
    # Output: State after confirm: open
    ```
  </Step>

  <Step title="Verify transfer was created">
    ```python
    # Check pickings
    pickings = request.picking_ids
    print(f"Number of transfers: {len(pickings)}")
    # Output: Number of transfers: 1
    
    picking = pickings[0]
    print(f"Picking: {picking.name}")
    print(f"State: {picking.state}")
    print(f"Source: {picking.location_id.name}")
    print(f"Destination: {picking.location_dest_id.name}")
    
    # Output:
    # Picking: INT/00001
    # State: assigned
    # Source: WH/Stock
    # Destination: Production Floor
    ```
  </Step>

  <Step title="Process the transfer">
    ```python
    # Validate the transfer
    picking.button_validate()
    
    # Check request status
    print(f"Request state: {request.state}")
    print(f"Qty done: {request.qty_done}")
    print(f"Qty in progress: {request.qty_in_progress}")
    
    # Output:
    # Request state: done
    # Qty done: 10.0
    # Qty in progress: 0.0
    ```
  </Step>
</Steps>

## Using Stock Request Orders

If you enabled Stock Request Orders, you can group multiple requests:

<Steps>
  <Step title="Navigate to Stock Request Orders">
    Stock Requests → Stock Request Orders → New
  </Step>

  <Step title="Fill in order header">
    Set common attributes for all requests in the order:
    - Warehouse
    - Location  
    - Expected Date
    - Shipping Policy
    - Requested By
  </Step>

  <Step title="Add request lines">
    In the **Stock Requests** tab, add lines:
    
    ```python
    order = env['stock.request.order'].create({
        'warehouse_id': env.ref('stock.warehouse0').id,
        'location_id': production_loc.id,
        'expected_date': fields.Datetime.now(),
        'stock_request_ids': [
            (0, 0, {
                'product_id': product1.id,
                'product_uom_qty': 5.0,
            }),
            (0, 0, {
                'product_id': product2.id,
                'product_uom_qty': 10.0,
            }),
            (0, 0, {
                'product_id': product3.id,
                'product_uom_qty': 3.0,
            }),
        ],
    })
    ```
  </Step>

  <Step title="Confirm the entire order">
    Click **Confirm** to process all requests at once.
    
    ```python
    order.action_confirm()
    
    # All child requests are confirmed
    for request in order.stock_request_ids:
        assert request.state == 'open'
    ```
  </Step>
</Steps>

## Monitoring Request Progress

### View Request Details

The request form shows real-time progress:

<Tabs>
  <Tab title="Status Fields">
    **State Badge**
    Shows current state with color coding:
    - Draft (gray)
    - In Progress (blue)
    - Done (green)
    - Cancelled (red)
    
    **Quantity Tracking**
    ```python
    # Display fields
    product_uom_qty    # Requested quantity
    qty_in_progress    # Being processed
    qty_done          # Completed
    qty_cancelled     # Cancelled
    ```
    
    **Progress Bar**
    Visual indicator of completion percentage
  </Tab>
  
  <Tab title="Smart Buttons">
    **Transfers**
    Click to view all stock pickings created by this request.
    
    ```python
    def action_view_transfer(self):
        pickings = self.mapped('picking_ids')
        # Opens picking list or form
    ```
    
    **Allocations**
    View stock move allocations linking requests to actual movements.
    
    ```python
    # Model: stock.request.allocation
    # Links: stock.request <-> stock.move
    ```
  </Tab>
  
  <Tab title="Related Documents">
    From the request, you can navigate to:
    - **Stock Moves**: All inventory movements
    - **Stock Pickings**: Transfer operations
    - **Purchase Orders**: If using purchase integration
    - **Manufacturing Orders**: If using MRP integration
    - **Procurement Group**: Related procurements
  </Tab>
</Tabs>

### List View Filters

Quickly find requests using built-in filters:

```python
# Common searches
# My requests
[('requested_by', '=', uid)]

# Open requests
[('state', '=', 'open')]

# Requests for specific product
[('product_id', '=', product_id)]

# Urgent requests (expected date soon)
[('expected_date', '<=', today + 7 days)]

# By warehouse
[('warehouse_id', '=', warehouse_id)]
```

## Cancelling Requests

<Steps>
  <Step title="Open the request">
    Navigate to the request you want to cancel
  </Step>

  <Step title="Click Cancel button">
    The system will:
    1. Cancel all pending stock moves
    2. Update move states to "cancelled"
    3. Set request state to "Cancelled"
    4. Update qty_cancelled field
    
    ```python
    def action_cancel(self):
        self.sudo().mapped('move_ids')._action_cancel()
        self.write({'state': 'cancel'})
    ```
  </Step>

  <Step title="Verify cancellation">
    <Warning>
    Cancelling a request does not:
    - Delete the request record
    - Cancel already completed transfers
    - Cancel confirmed purchase orders (must cancel separately)
    - Refund or return already received stock
    </Warning>
  </Step>
</Steps>

## Common Workflows

### Workflow 1: Request with Available Stock

<CodeGroup>
  ```mermaid Workflow
  graph LR
    A[Create Request] --> B[Confirm]
    B --> C{Stock Available?}
    C -->|Yes| D[Create Transfer]
    D --> E[Process Transfer]
    E --> F[Request Done]
  ```

  ```python Code
  # Enable check available first
  company.stock_request_check_available_first = True
  
  # Create request
  request = env['stock.request'].create({...})
  
  # Confirm - will use available stock
  request.action_confirm()
  
  # System creates and processes transfer immediately
  assert request.picking_ids
  assert request.state == 'open'
  
  # Complete transfer
  request.picking_ids[0].button_validate()
  
  # Request auto-completes
  assert request.state == 'done'
  ```
</CodeGroup>

### Workflow 2: Request Triggering Purchase

<CodeGroup>
  ```mermaid Workflow
  graph LR
    A[Create Request] --> B[Confirm]
    B --> C{Stock Available?}
    C -->|No| D{Route Action?}
    D -->|Buy| E[Create PO]
    E --> F[Approve PO]
    F --> G[Receive Goods]
    G --> H[Request Done]
  ```

  ```python Code
  # Requires: stock_request_purchase module
  
  # Assign buy route to product
  product.route_ids = [(4, buy_route.id)]
  
  # Create request
  request = env['stock.request'].create({
      'product_id': product.id,
      'product_uom_qty': 100.0,
      'location_id': location.id,
  })
  
  # Confirm - triggers purchase
  request.action_confirm()
  
  # Purchase order is created
  po = env['purchase.order'].search([
      ('origin', 'ilike', request.name)
  ])
  assert po
  
  # After receiving PO, request completes
  ```
</CodeGroup>

### Workflow 3: Request Triggering Manufacturing

<CodeGroup>
  ```mermaid Workflow
  graph LR
    A[Create Request] --> B[Confirm]
    B --> C{Product Type?}
    C -->|Manufactured| D[Create MO]
    D --> E[Process MO]
    E --> F[Complete Production]
    F --> G[Request Done]
  ```

  ```python Code
  # Requires: stock_request_mrp module
  
  # Assign manufacture route
  product.route_ids = [(4, manufacture_route.id)]
  
  # Ensure BOM exists
  bom = env['mrp.bom'].search([('product_tmpl_id', '=', product.product_tmpl_id.id)])
  assert bom
  
  # Create request
  request = env['stock.request'].create({
      'product_id': product.id,
      'product_uom_qty': 50.0,
      'location_id': location.id,
  })
  
  # Confirm - triggers manufacturing
  request.action_confirm()
  
  # Manufacturing order is created
  mo = env['mrp.production'].search([
      ('origin', 'ilike', request.name)
  ])
  assert mo
  ```
</CodeGroup>

## Tips and Best Practices

<CardGroup cols={2}>
  <Card title="Use Expected Dates" icon="calendar">
    Always set realistic expected dates to help warehouse teams prioritize fulfillment.
  </Card>
  
  <Card title="Group Related Requests" icon="layer-group">
    Use Stock Request Orders to group requests for the same project or purpose.
  </Card>
  
  <Card title="Configure Routes Properly" icon="route">
    Ensure routes are configured before requesting products, or procurement may fail.
  </Card>
  
  <Card title="Monitor Progress" icon="chart-line">
    Regularly check your open requests and follow up on delays.
  </Card>
</CardGroup>

<Tip>
**Pro tip**: Enable "Check Available Stock First" if you want to prioritize using existing inventory before triggering new procurement.
</Tip>

## Troubleshooting

<AccordionGroup>
  <Accordion title="Request confirmed but no transfer created">
    **Possible causes:**
    1. No route configured for the product/location combination
    2. Product type is not "Storable Product"
    3. Procurement rules misconfigured
    
    **Solution:**
    ```python
    # Check procurement error
    try:
        request.action_confirm()
    except UserError as e:
        print(f"Error: {e}")
    
    # Verify routes
    routes = request._get_routes()
    print(f"Available routes: {routes.mapped('name')}")
    ```
  </Accordion>

  <Accordion title="Cannot see my request after creating it">
    **Cause:** Security rules filter requests by creator.
    
    **Solution:**
    - As a User: You can only see requests you created or are following
    - As a Manager: You can see all requests
    - Ask a manager to add you as follower on specific requests
  </Accordion>

  <Accordion title="Request stuck in 'In Progress' state">
    **Cause:** Transfers not completed or quantities don't match.
    
    **Solution:**
    ```python
    # Check quantities
    print(f"Requested: {request.product_uom_qty}")
    print(f"Done: {request.qty_done}")
    print(f"In progress: {request.qty_in_progress}")
    print(f"Cancelled: {request.qty_cancelled}")
    
    # Manually complete if needed
    if request.qty_done >= request.product_uom_qty:
        request.action_done()
    ```
  </Accordion>
</AccordionGroup>

## Next Steps

<CardGroup cols={3}>
  <Card title="Integrations" icon="wand-magic-sparkles" href="/guides/integrations">
    Learn about tier validation, Purchase, MRP, and automation
  </Card>
  
  <Card title="API Reference" icon="code" href="/api/stock-request">
    Explore the complete API documentation
  </Card>
  
  <Card title="Procurement Rules" icon="star" href="/guides/procurement-rules">
    Learn recommended procurement patterns and workflows
  </Card>
</CardGroup>